<templateSet group="Android">
  <template name="SiblyAPI" value="private void __() {&#10;&#10;        wsRequestsManager = new WSRequestsManager();&#10;        String[] pathSegment = new String[2];&#10;        pathSegment[0] = Constants.API;&#10;        pathSegment[1] = Constants.___;&#10;&#10;        JSONObject jsonObjectBody = new JSONObject();&#10;        try {&#10;            jsonObjectBody.put(ReferenceUrl.KEY_USER_ID, Prefs.getPref(Prefs.KEY_USER_ID, act));&#10;            jsonObjectBody.put(ReferenceUrl.KEY_Token, Prefs.getPref(Prefs.KEY_TOKEN, act));&#10;            jsonObjectBody.put(ReferenceUrl.KEY_CHANNEL_URL, Prefs.getPref(Prefs.KEY_CHANNEL_URL, act));&#10;            jsonObjectBody.put(ReferenceUrl.__, __);&#10;            jsonObjectBody.put(ReferenceUrl.__, __);&#10;&#10;&#10;        } catch (JSONException e) {e.printStackTrace();}&#10;&#10;&#10;        wsRequestsManager.sendRequest(pathSegment, null, jsonObjectBody, WSRequestsManager.POST, null, act,&#10;                new WSRequestsManager.OnRequestResponselistener() {&#10;                    @Override&#10;                    public void onRequestResponse(final String mResponse) {&#10;&#10;                        if (mResponse == null) {&#10;                            //if fail&#10;                            act.runOnUiThread(new Runnable() {&#10;                                @Override&#10;                                public void run() {&#10;                                    //showProgressBar(false);&#10;                                    Utils.showInfoDialog(act, &quot;&quot;, act.getString(R.string.error_connection), true, act.getString(R.string.OK), null);&#10;                                }&#10;                            });&#10;&#10;                        } else {&#10;&#10;                            try {&#10;                                JSONObject json = new JSONObject(mResponse);&#10;                                //action here&#10;&#10;&#10;&#10;                            } catch (JSONException e) { e.printStackTrace(); }&#10;&#10;                        }&#10;&#10;                    }&#10;                });" description="Create a Sibly API call Template." toReformat="false" toShortenFQNames="true">
    <context>
      <option name="JAVA_STATEMENT" value="true" />
      <option name="JAVA_EXPRESSION" value="true" />
      <option name="JAVA_DECLARATION" value="true" />
    </context>
  </template>
</templateSet>